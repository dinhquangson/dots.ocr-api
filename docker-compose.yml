services:
  api:
    build: .
    ports: ["8000:8000"]
    environment:
      - MODEL_ID=rednote-hilab/dots.ocr
      - TORCH_DTYPE=float32
    volumes:
      - ./app:/app
      - hf_cache:/root/.cache/huggingface
volumes:
  hf_cache: {}
